# make sure you have all files in the same folder
# the run the following command : docker image build -t hello:v0.1 .

# make sure you take the last dot in the above command, otherwise you will not get
# the expected result
